describe('List', () => {
	describe('Get lists', () => {
		it('should return 200 OK', () => {
			expect(1).toBe(1);
		});
	});
});

describe('List', () => {
	describe('Get list', () => {
		it('should return 200 OK', () => {
			expect(1).toBe(1);
		});
	});
});
